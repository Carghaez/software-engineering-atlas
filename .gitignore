# Generated by the build at deploy time (GitHub Actions) — never committed.
# Source of truth: concepts/<id>/concept.json + build.mjs. Run `npm run build` to preview.
/c/
/sitemap.xml
/code-index.json

# Committed-but-generated (kept in git so the app runs with no build); a CI drift
# test keeps it in sync with concepts/<id>/concept.json. Do not hand-edit:
#   concepts.data.js

# Node
node_modules/
